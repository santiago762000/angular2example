import { ConfirmactionDirective } from './confirmaction.directive';

describe('ConfirmactionDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmactionDirective();
    expect(directive).toBeTruthy();
  });
});
